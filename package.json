{
  "name": "arrowy-env",
  "version": "1.0.0",
  "description": "function which access to an env variables located in the .env file in the root folder.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "build:dev": "npm run build -- --watch",
    "test": "jest --config jest.config.js tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "29.5.12",
    "jest": "29.7.0",
    "ts-jest": "29.1.2"
  }
}
